class Solution {
public:
    string largestEven(string s) {
        int n =s.size();
        int j =n-1;
        while( j >=0){
            if((s[j]-'0') %2==0)break;
            else j--;
        }
        return s.substr(0 , j+1);
    }
};
