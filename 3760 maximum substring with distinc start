class Solution {
public:
    int maxDistinct(string s) {
       int cnt=0;
       unordered_set<char>st;
       for(char c: s){
        st.insert(c);
       }
   return st.size();
    }
};
tc =O(N)
SC=O(1)
